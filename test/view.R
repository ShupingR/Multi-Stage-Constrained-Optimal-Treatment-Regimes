load('out1.Rdata')
T11 <- T1.pred
X21 <- X2.pred
T21 <- T2.pred
Y1 <- Y.pred
Z1 <- Z.pred

rm(T1.pred)
rm(X2.pred)
rm(T2.pred)
rm(Y.pred)
rm(Z.pred)

load('out2.Rdata')
T12 <- T1.pred
X22 <- X2.pred
T22 <- T2.pred
Y2 <- Y.pred
Z2 <- Z.pred

rm(T1.pred)
rm(X2.pred)
rm(T2.pred)
rm(Y.pred)
rm(Z.pred)