This folder shows that smoothed bootstrap is equivalent to draw samples from estimated kernel density.

The bandwidth choice for multivariate is following Scott’s rule in blue slide. The slide also provides a sample size consideration. 